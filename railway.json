{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "buildCommand": "docker build -t vinted-app ."
  },
  "deploy": {
    "startCommand": "frankenphp run --bind=0.0.0.0:8000",
    "restartPolicyMaxRetries": 5
  }
}
